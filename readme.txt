Terje Gundersen					01/07/2009

1. New recording
 1a. Place the .wav file in data/rec directory
 1b. Label the file with Praat and place the .TextGrid file in 
     the data/textgrid directory
     Make sure the name of the Praat project(can be changed in 
     the .TextGrid file) is the same as the .wav file
2. Run 'python script_prepare_training.py'
3. Run 'bash script_training.sh'
